# Copy to .env.local for local development, and set in the Vercel project settings
# for production. Optional: without it, the URL is derived from Vercel's own env vars
# (see src/lib/metadata.ts).
NEXT_PUBLIC_SITE_URL=https://example.com
